# v20 baseline, gap register & Final verdict (SV-00 / SV-FINAL)

**Branch:** `v20`  · **Baseline HEAD inspected:** `47418d3` (matches the pack's
reviewed SHA exactly — no drift) · **Prepared:** 2026-08-18 · Europe/Ljubljana

This records the SV-00 re-baseline and the SV-FINAL computed verdict for the
Scalvya (`primocera/LaunchBloom`) side of the *Final Launch Prompt Pack v20*. The
Mellowa `MW-*` prompts target the separate `primocera/Mellowa` repository and are
**out of scope** here. Owner-only live steps remain **NOT RUN**.

## SV-00 — non-secret gate at branch v20 (re-run, not carried forward)

| Check | Command | Result |
|---|---|---|
| Lint | `npm run lint` | **0 errors / 0 warnings** |
| Unit / contract / safety | `npm test` | **978 pass / 0 fail / 0 skip** (was 967; +11 SV-01 ownership-service suites) |
| Production build | `npm run build:app` | built; bundle `index-Cq2NTdSE` **unchanged** (no frontend source touched) |
| Bundle freshness | `npm run check:app-fresh` | `app/` up to date with `app-src/` |
| Launch-state integrity | `npm test` (launch-state suite) | 44/44 pass; manifest internally consistent |

Environment class: **non-secret local**. Not run here (owner/secret-only):
authenticated seeded E2E matrix, production readiness observation, immutable RC
workflow, live-money rehearsal, capacity load runs.

## Gap register — every v20 prompt, evidence-classified

| Prompt | Scope | Status | Evidence |
|---|---|---|---|
| **SV-00** | Re-baseline + gap register | **DONE (this doc)** | gate above |
| **SV-01** | Explicit Stripe object ownership | **CODE/SCHEMA DONE; 2 owner steps NOT RUN** | see below |
| **SV-02** | Immutable RC at exact SHA | **ALREADY SATISFIED** | `.github/workflows/release-candidate.yml` (RC_GATE, fail-closed, SHA+run-id artifacts); owner triggers it |
| **SV-03** | Readiness pinned to deployed candidate | **ALREADY SATISFIED (evidence layer)** | `lib/readiness-evidence.js validateReadinessRecord` rejects a record whose `candidate_sha` ≠ pinned candidate; readiness roll-up fails closed on unmeasured signals (v19 LB-05). Residual owner/config: inject the running VCS SHA (e.g. `VERCEL_GIT_COMMIT_SHA`) for endpoint self-report |
| **SV-04** | A-H live-money rehearsal | **CODE/RUNBOOK DONE; live run OWNER-ONLY, NOT RUN** | `docs/RUNBOOK_TRANSACTION_REHEARSAL.md`, `lib/rehearsal.js`, `scripts/validate-rehearsal.js`; the live 8-transition run is the standing accepted-risk `live_money_rehearsal` |
| **SV-05** | Real 10x capacity + SLOs | **MODEL DONE; measured numbers OWNER-ONLY** | `docs/CAPACITY_MODEL_V19.md` (UNAVAILABLE items + the measurement to close each), `docs/SLOS.md`; synthetic load runs are owner-run |
| **SV-06** | Canonical beta value decision | **ALREADY SATISFIED** | `lib/beta-scorecard.js`, `lib/weekly-decision.js`, `lib/cohort*.js`; one canonical funnel + weekly decision; real cohort stays PENDING (not customer evidence) |
| **SV-FINAL** | Truthful launch certification | **DONE (this doc)** | verdicts below |
| **XAPP-01** | Scalvya-side shared-Stripe isolation | **ALREADY SATISFIED** | `tests/cross-app-isolation.test.js` (10) + `tests/xapp-account-isolation.test.js` (2); the two-repo matrix additionally needs the Mellowa repo |
| `MW-*`, `XAPP-FINAL` | Mellowa / cross-repo | **OUT OF SCOPE** | separate `primocera/Mellowa` repository |

### SV-01 — what v20 built (branch `v20`)

The ownership-architecture trio deferred by owner decision at v19 is now landed at
the code/schema/tooling layer, additively, with zero runtime behaviour change
(978/978, incl. all 55 billing/isolation suites):

- **One canonical typed service** `backend/lib/stripe-ownership.js` — typed states
  `owned/foreign/legacy_mapped/legacy_price/ambiguous/unavailable`; `webhooks.js`,
  `payments.js` and the reconciler delegate to it. Email/price are never
  sufficient proof; conflicting exact+foreign stamps are `ambiguous`, never adopted.
- **Additive migration** `backend/migrations/038_stripe_object_ownership.sql`
  (`customers.app_user_id` + `stripe_object_ownership` legacy map) with
  preflight/apply/verify/rollback — **NOT APPLIED** (owner step).
- **Owner backfill/inventory** `backend/scripts/backfill-stripe-ownership.js`
  (read-only default, bounded `--apply`, resume cursor, PII-free receipt).
- **Runbook** `docs/RUNBOOK_STRIPE_OWNERSHIP.md`.

**Owner-only, NOT RUN:** (1) apply migration 038 + re-run `CHECK_APPLIED.sql`;
(2) run the backfill and reconcile every `ambiguous` row, then flip the price-only
fallback off. Until then the runtime keeps the fallback (every use measured by the
`legacy_price_ownership_fallback` ops-signal).

## SV-FINAL — five computed verdicts (evidence-grounded)

| Tier | Verdict | Rests on |
|---|---|---|
| Automated code gate | **PASS** at v20 | lint 0/0, 978/978, build fresh, launch integrity |
| Capped beta | **GO** (unchanged) | manifest `capped_beta` GO; no code regression |
| Bounded paid | **CONDITIONAL** | inherits public-paid accepted risks; unchanged by v20 |
| Unrestricted public paid | **CONDITIONAL GO** (unchanged) | still rests only on the two accepted risks (router advisory, live-money). SV-01 now removes the *architecture* blocker at the code layer, but the fallback-sunset + migration-apply remain owner steps, so this does **not** advance to full GO |
| Scale / paid acquisition | **BLOCKED** | capacity numbers UNAVAILABLE + no mature real cohort — both owner-measured |

**Not certified for any tier by this doc:** unrestricted-paid full GO, and scale.
Those require the owner-only evidence above. v20 changed backend billing/ops code
and added migration 038, so a **new RC must be cut** (SV-02 workflow) before the
manifest candidate advances past `24d350c`; this baseline does not freeze one.
