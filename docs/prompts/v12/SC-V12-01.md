# SC-V12-01 · Scalvya — Make launch verdicts truthful: GO, CONDITIONAL GO and NO-GO

**Run after:** first Scalvya prompt (start of pack).
**Primary risk removed:** a public launch displayed as full GO even though authenticated E2E,
WCAG contrast and the complete live-money rehearsal remain unverified.

## Prompt

You are working in primocera/LaunchBloom on the latest default branch. Fix the launch-control
semantics without changing product behavior.

Current problem to verify first:
- docs/launch/launch-state.json is the canonical source.
- The current release record can compute public_paid = GO while required P0/P1 conditions remain
  unresolved but carry accepted_risk records.
- The rendered document can say "Open blockers: None" even though accepted blockers remain.
- A check such as hero_contrast may be technically green because it pins a known failure floor
  rather than proving WCAG compliance.

Required implementation:
1. Inspect backend/lib/launch-state.js, backend/scripts/launch-state.js,
   backend/tests/launch-state.test.js, docs/launch/launch-state.json and docs/LAUNCH_STATE.md.
2. Introduce exactly three verdicts:
   - GO: every required P0/P1 condition for the track is actually satisfied by valid evidence.
   - CONDITIONAL GO: no unaccepted blocking condition remains, but one or more required
     conditions are bypassed through a valid accepted_risk for that track.
   - NO-GO: at least one required condition is failed, skipped, blocked, unknown, not_run, stale
     or otherwise unsatisfied without a valid scoped acceptance.
3. An accepted risk must never turn the underlying status into passed or closed.
4. Add a closed verdict vocabulary and reject unknown verdict strings during manifest integrity
   validation.
5. Return an accepted_risks list from verdict computation. Deduplicate the same logical risk when
   it appears in blocker, check and owner-evidence records.
6. Render "Unresolved blockers and accepted risks", showing open and accepted items with status,
   severity, owner and closure requirement.
7. Ensure full GO is impossible with any accepted required P0/P1 risk.
8. Update the canonical manifest's declared verdicts only as required to match the new
   deterministic computation. Do not invent new evidence or close any existing item.
9. Regenerate docs/LAUNCH_STATE.md through the repository renderer. Do not hand-edit generated
   output.
10. Update every current handoff/readme statement that incorrectly calls accepted-risk launch a
    full GO.

Required tests:
- A fully evidenced fixture returns GO.
- One correctly scoped accepted required risk returns CONDITIONAL GO for only that track.
- Removing the acceptance returns NO-GO.
- A malformed acceptance is rejected.
- A declared GO that should be CONDITIONAL GO fails integrity.
- The real committed manifest is internally consistent.
- Rendered launch-state output lists accepted risks and never says "all conditions met" for
  CONDITIONAL GO.

Constraints:
- Do not change pricing, entitlements, production configuration or owner evidence.
- Do not soften the distinction between local_pass, passed_ci, live_rehearsed and observed.
- Do not make launch:gate exit successfully for a CONDITIONAL GO if the command is defined as the
  full-GO release gate.

Finish with the exact old verdict, new verdict, risks keeping it conditional, changed files and
test output.
