# SC-P1-09 · Current-head release candidate and evidence gate

PriorityDepends on
P1 / blockingSC-P0 and SC-P1 correctness prompts
OutcomeRun mode
A truthful candidate with zero code driftOne focused branch/PR; stop on unsafe or production-only steps
PASTE INTO CLAUDE CODE
ROLE AND OBJECTIVE
Cut and verify a new Scalvya release candidate only after all code fixes are merged. The previous pinned candidate 1e465433ac7f4d78ecb7d80fa2a47aac11880bb8 is stale and the launch gate correctly reports NO-GO because code changed after it.
EXECUTION REQUIREMENTS
Start from clean main. Record exact HEAD, dependency lock hash, build artifact hash if the repo supports it, environment class, and timestamp.
Run: npm ci; npm run lint; npm test; npm run build:app; npm run check:app-fresh; npm run test:export; npm run test:e2e; and the approved authenticated E2E command when its non-production prerequisites exist.
Run read-only price/config verification where safe. Any missing owner environment remains BLOCKED.
Generate/update evidence using repository scripts—npm run release:evidence, npm run launch:verify, npm run launch:drift, and npm run launch:gate—not hand-edited verdict text.
Understand semantics: launch:verify proves manifest integrity; launch:gate determines readiness based on required evidence and drift. Do not report verify success as GO.
If any source/config/lockfile changes after the candidate is pinned, invalidate the candidate and repeat. Documentation-only changes may be handled only according to the repository's explicit drift policy.
EVIDENCE LABELS
Use only precise statuses such as passed_local, passed_ci, observed_test_mode, observed_live, blocked, failed, accepted_risk, and not_run if supported by the schema.
Include direct artifact/run references where available. Never paste secrets or customer PII.
ACCEPTANCE CRITERIA
launch:gate is green for the intended tier on the exact candidate commit, or the output remains a truthful NO-GO/BLOCKED.
Public E2E and auth E2E are current-head evidence; old 52-test results are not reused after code changes.
The frontend production bundle is freshly generated and integrity-checked.
STOP CONDITION
Do not deploy, take a live payment, change production variables, or manually override the machine verdict. Prepare an owner checklist for those steps.
FINAL REPORT
Return: (1) exact files changed, (2) behavior before and after, (3) tests run with exact pass/fail counts, (4) commands not run and why, (5) residual risks, (6) documentation/evidence updated, and (7) whether the next prompt is safe to start.
Use PASS only for a command or production observation that actually ran against the stated commit and environment. Use BLOCKED, NOT RUN, or FAILED otherwise.
Do not claim launch readiness in prose. Report evidence; the repository's release gate and the owner determine the verdict.
END OF PROMPT
