# SC-P1-07 · Zero-warning React correctness pass

PriorityDepends on
P1P0 prompts complete
OutcomeRun mode
Reliable studio reloads and clean product-code lintOne focused branch/PR; stop on unsafe or production-only steps
PASTE INTO CLAUDE CODE
ROLE AND OBJECTIVE
Reduce the current lint baseline from 59 warnings to zero for product code, prioritizing React Hook correctness rather than cosmetic suppression. Fix the four studio effects with missing load dependencies so campaign/query changes reload deterministically.
IMPLEMENTATION REQUIREMENTS
Classify every warning: correctness, accessibility, unsafe async behavior, dead code, or test-only noise. Do not add blanket eslint-disable rules.
For studio load functions, use useCallback or a stable extracted data hook with the complete dependency list. Prevent duplicate requests, stale closures, races, state updates after unmount, and accidental reload loops.
When route/campaign/query identifiers change, cancel or ignore stale responses and display existing loading/error/empty states correctly.
Fix remaining warnings with minimal code changes. Keep test-specific lint policy separate only when justified in config and documented.
TESTS
Component/integration tests for switching campaigns or query IDs without remounting.
Slow-response race test: old campaign data cannot overwrite the new campaign.
npm run lint must finish with zero product-code warnings and zero errors.
Run npm test, build:app, and check:app-fresh.
ACCEPTANCE CRITERIA
No disabled dependency rules hide the studio issue.
Studios reload exactly when their authoritative inputs change.
No stale campaign data is shown after navigation.
FINAL REPORT
Return: (1) exact files changed, (2) behavior before and after, (3) tests run with exact pass/fail counts, (4) commands not run and why, (5) residual risks, (6) documentation/evidence updated, and (7) whether the next prompt is safe to start.
Use PASS only for a command or production observation that actually ran against the stated commit and environment. Use BLOCKED, NOT RUN, or FAILED otherwise.
Do not claim launch readiness in prose. Report evidence; the repository's release gate and the owner determine the verdict.
END OF PROMPT
